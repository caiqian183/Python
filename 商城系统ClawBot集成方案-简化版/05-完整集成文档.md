# 商城系统ClawBot集成方案（简化版）

## 1. 系统架构

### 1.1 整体架构

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│   微信用户   │ ───> │ ClawBot网关 │ ───> │ 商城系统API │ ───> │ 数据库存储  │
└─────────────┘      └─────────────┘      └─────────────┘      └─────────────┘
       ↑                    ↑                    │                    │
       │                    │                    │                    │
       └────────────────────┘ <──────────────────┘                    │
                                    消息回调                         │
                                                                    │
       ┌─────────────┐      ┌─────────────┐                         │
       │ 前端界面    │ <─── │ 商城系统API │ <───────────────────────┘
       └─────────────┘      └─────────────┘
```

### 1.2 核心组件

1. **微信用户**：使用微信发送消息的用户
2. **ClawBot网关**：处理微信消息的接收和转发
3. **商城系统API**：接收Webhook回调，处理消息存储和业务逻辑
4. **数据库**：存储用户绑定信息和消息历史
5. **前端界面**：用户绑定管理和消息查看界面

## 2. 技术栈

| 组件 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 后端 | FastAPI | 0.104.1 | 提供RESTful API |
| 前端 | HTML5 + CSS3 + JavaScript | - | 绑定管理界面 |
| 数据库 | SQLite | - | 数据存储（可扩展为MySQL） |
| 网关 | ClawBot | - | 微信消息处理 |
| 依赖 | Python 3.8+ | - | 运行环境 |

## 3. 数据库设计

### 3.1 表结构

#### 用户表 (users)
| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | UNIQUE NOT NULL | 用户名 |
| email | VARCHAR(100) | UNIQUE NOT NULL | 邮箱 |
| password_hash | VARCHAR(255) | NOT NULL | 密码哈希 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### ClawBot绑定表 (clawbot_bindings)
| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 绑定ID |
| user_id | INT | NOT NULL, FOREIGN KEY | 用户ID |
| wechat_bot_token | VARCHAR(255) | NULL | 微信机器人令牌 |
| wechat_user_id | VARCHAR(255) | NULL | 微信用户ID |
| wechat_nickname | VARCHAR(100) | NULL | 微信昵称 |
| binding_status | ENUM | DEFAULT 'pending' | 绑定状态 |
| qr_code_data | TEXT | NULL | 二维码数据 |
| qr_code_expires_at | DATETIME | NULL | 二维码过期时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 微信消息表 (wechat_messages)
| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 消息ID |
| binding_id | INT | NOT NULL, FOREIGN KEY | 绑定ID |
| message_id | VARCHAR(255) | NULL | 消息ID |
| content | TEXT | NULL | 消息内容 |
| sender_id | VARCHAR(255) | NULL | 发送者ID |
| sent_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 发送时间 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### ClawBot网关配置表 (clawbot_gateway_config)
| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | INT | PRIMARY KEY AUTO_INCREMENT | 配置ID |
| config_key | VARCHAR(100) | UNIQUE NOT NULL | 配置键 |
| config_value | TEXT | NULL | 配置值 |
| description | TEXT | NULL | 描述 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 3.2 初始数据

```sql
-- 初始配置数据
INSERT INTO clawbot_gateway_config (config_key, config_value, description) VALUES
('gateway_url', 'http://localhost:8765', 'ClawBot网关URL'),
('admin_token', 'your-secret-admin-token', '网关管理令牌'),
('webhook_url', 'https://your-domain.com/api/clawbot/webhook', 'Webhook回调URL'),
('webhook_api_key', 'your-webhook-api-key', 'Webhook API密钥');
```

## 4. 后端API实现

### 4.1 主要API端点

| 端点 | 方法 | 功能 | 权限 |
|------|------|------|------|
| `/api/clawbot/bind/init/{user_id}` | POST | 生成绑定二维码 | 需认证 |
| `/api/clawbot/bind/status/{user_id}` | GET | 检查绑定状态 | 需认证 |
| `/api/clawbot/unbind/{user_id}` | POST | 解绑ClawBot | 需认证 |
| `/api/clawbot/webhook` | POST | 接收微信消息 | 网关回调 |
| `/api/clawbot/messages/{user_id}` | GET | 获取消息历史 | 需认证 |

### 4.2 核心代码

#### Webhook端点（消息接收）

```python
@app.post("/api/clawbot/webhook")
async def clawbot_webhook(request: Request, db: Session = Depends(get_db)):
    """接收来自ClawBot网关的Webhook"""
    # 验证Webhook签名
    api_key = request.headers.get("X-API-Key")
    expected_api_key = GatewayConfig(db).get_webhook_api_key()
    
    if api_key != expected_api_key:
        raise HTTPException(status_code=401, detail="Invalid API key")
    
    # 处理消息
    data = await request.json()
    message_type = data.get("type")
    payload = data.get("data", {})
    
    if message_type == "message":
        # 处理收到的微信消息
        wechat_user_id = payload.get("from_user_id")
        message_content = payload.get("content")
        message_id = payload.get("message_id")
        
        # 查找对应的绑定
        binding = db.query(ClawBotBinding).filter(
            ClawBotBinding.wechat_user_id == wechat_user_id
        ).first()
        
        if binding:
            # 记录消息
            message = WechatMessage(
                binding_id=binding.id,
                message_id=message_id,
                content=message_content,
                sender_id=wechat_user_id
            )
            db.add(message)
            db.commit()
            
            # 这里可以添加业务逻辑，比如处理用户的消息
            # 例如：将消息存入数据库、触发业务流程等
            print(f"收到微信消息 from {wechat_user_id}: {message_content}")
    
    # 不需要回复，只需要记录
    return {"success": True}
```

## 5. 前端界面实现

### 5.1 功能模块

1. **绑定管理**：生成二维码、扫码绑定、解绑功能
2. **状态显示**：显示当前绑定状态和微信用户信息
3. **消息历史**：查看收到的微信消息历史
4. **功能说明**：提供系统功能和使用说明

### 5.2 界面设计

- **响应式布局**：适配不同屏幕尺寸
- **用户友好**：清晰的视觉层次和操作流程
- **实时反馈**：绑定状态实时更新和倒计时显示
- **错误处理**：友好的错误提示和异常处理

## 6. ClawBot网关配置

### 6.1 配置文件

```yaml
# 服务器配置
server:
  host: 0.0.0.0
  port: 8765
  debug: false

# 认证配置
auth:
  admin_token: your-secret-admin-token
  webhook_api_key: your-webhook-api-key

# 回调配置
webhook:
  url: https://your-domain.com/api/clawbot/webhook
  timeout: 10
  retry: 3

# 多用户配置
multi_user:
  enabled: true
  routing:
    by_wechat_id: true
    by_content: false

# 插件配置
plugins:
  enabled: []
```

### 6.2 启动命令

```bash
# 启动网关
python main.py

# 后台运行
nohup python main.py > gateway.log 2>&1 &
```

## 7. 集成步骤

### 7.1 准备工作

1. 安装Python 3.8+环境
2. 下载ClawBot网关软件
3. 准备数据库（SQLite或MySQL）
4. 配置域名和HTTPS（生产环境）

### 7.2 部署步骤

1. **部署商城系统API**
   - 安装依赖：`pip install -r requirements.txt`
   - 启动服务：`python 02-后端API示例.py`

2. **配置ClawBot网关**
   - 编辑 `gateway.yaml` 文件
   - 启动网关服务

3. **配置Webhook**
   - 在网关配置中设置Webhook URL
   - 确保Webhook URL可访问

4. **测试集成**
   - 打开前端界面 `03-前端绑定界面.html`
   - 生成二维码并扫码绑定
   - 发送微信消息测试

5. **监控和维护**
   - 查看网关日志
   - 监控API响应
   - 定期备份数据库

## 8. 测试方法

### 8.1 功能测试

1. **绑定流程测试**
   - 生成二维码
   - 扫码绑定
   - 检查绑定状态
   - 解绑测试

2. **消息接收测试**
   - 发送微信消息
   - 检查消息是否存储到数据库
   - 查看前端消息历史

3. **异常测试**
   - 二维码过期测试
   - Webhook认证失败测试
   - 网络中断测试

### 8.2 性能测试

1. **并发测试**：模拟多个用户同时绑定
2. **消息处理测试**：模拟大量消息推送
3. **响应时间测试**：测试API响应速度

## 9. 故障排查

### 9.1 常见问题

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| 二维码生成失败 | 网关配置错误 | 检查网关URL和令牌 |
| 绑定状态更新失败 | 网络连接问题 | 检查网络连接和网关响应 |
| 消息接收失败 | Webhook配置错误 | 检查Webhook URL和API密钥 |
| 数据库错误 | 表结构不存在 | 运行数据库初始化脚本 |
| 前端界面无法访问 | API服务未启动 | 检查服务状态和端口 |

### 9.2 日志分析

- **网关日志**：`logs/clawbot-gateway.log`
- **API日志**：控制台输出或配置的日志文件
- **数据库日志**：根据数据库类型查看相应日志

## 10. 安全建议

### 10.1 安全措施

1. **使用HTTPS**：所有API和Webhook通信使用HTTPS
2. **强密码**：使用强随机的API密钥和令牌
3. **IP限制**：限制网关API的访问IP
4. **数据加密**：对敏感数据进行加密存储
5. **日志监控**：启用详细的日志记录和监控

### 10.2 最佳实践

1. **定期更新**：定期更新依赖库和网关版本
2. **备份数据**：定期备份数据库和配置文件
3. **访问控制**：实现严格的用户认证和授权
4. **异常处理**：完善的错误处理和异常捕获
5. **代码审计**：定期进行代码安全审计

## 11. 扩展和定制

### 11.1 功能扩展

1. **消息通知**：添加邮件或短信通知
2. **消息处理**：根据消息内容触发业务流程
3. **多渠道集成**：集成其他消息平台
4. **数据分析**：添加消息数据分析功能

### 11.2 技术扩展

1. **数据库升级**：从SQLite升级到MySQL或PostgreSQL
2. **缓存优化**：添加Redis缓存提升性能
3. **负载均衡**：使用负载均衡提高系统可用性
4. **容器化部署**：使用Docker容器化部署

## 12. 总结

本方案提供了一个完整的商城系统ClawBot集成解决方案，专注于接收用户在微信发送的消息。通过本方案，您可以：

1. 实现微信ClawBot的绑定和管理
2. 接收并存储用户在微信发送的消息
3. 在系统中查看消息历史记录
4. 建立微信与商城系统的通信桥梁

本方案采用了现代化的技术栈，具有良好的可扩展性和可维护性。您可以根据实际需求进行调整和扩展，以满足特定的业务场景。

## 13. 附录

### 13.1 依赖项列表

```
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.2
pydantic-settings==2.1.0
sqlalchemy==2.0.23
httpx==0.25.2
qrcode==7.4.2
pillow==10.1.0
python-dotenv==1.0.0
```

### 13.2 系统流程图

```
1. 用户访问商城系统个人中心
2. 点击"生成二维码"按钮
3. 系统调用ClawBot网关生成二维码
4. 用户使用微信扫描二维码
5. 微信确认绑定
6. ClawBot网关通知系统绑定成功
7. 系统更新绑定状态
8. 用户在微信发送消息
9. ClawBot网关通过Webhook推送消息
10. 系统接收并存储消息
11. 用户在系统中查看消息历史
```

### 13.3 技术支持

- **ClawBot官方文档**：请参考ClawBot官方文档获取更多信息
- **FastAPI文档**：https://fastapi.tiangolo.com/
- **SQLAlchemy文档**：https://docs.sqlalchemy.org/

### 13.4 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| 1.0.0 | 2024-01-01 | 初始版本，专注于消息接收功能 |